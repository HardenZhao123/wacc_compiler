# a basic try-catch should catch the exception and continue run after try-catch

# Output:
# caught
# survived

# Program:

begin
  try
    throw NullDereferenceException("null")
  catch NullDereferenceException e do
    println "caught"
  done;
  println "survived"
end