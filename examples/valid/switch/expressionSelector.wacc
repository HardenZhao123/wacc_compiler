# switch on the result of an expression

# Output:
# expression matched
#

# Program:

begin
  int x = 2;
  switch (x + 1)
    case 1: println "unexpected"; Break
    case 3: println "expression matched"; Break
    default: println "no match"
  end
end
