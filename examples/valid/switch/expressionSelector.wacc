# switch on the result of an expression

# Output:
# expression matched
#

# Program:

begin
  int x = 2;
  switch (x + 1)
    case 1: println "unexpected"
    case 3: println "expression matched"
    default: println "no match"
  end
end
