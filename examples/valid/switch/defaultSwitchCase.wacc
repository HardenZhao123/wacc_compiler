# use the default case when no label matches

# Output:
# default selected
#

# Program:

begin
  int value = 99;
  switch (value)
    case 1: println "first case"
    case 2: println "second case"
    default: println "default selected"
  end
end
