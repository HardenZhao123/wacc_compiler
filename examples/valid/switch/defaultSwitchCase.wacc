# use the default case when no label matches

# Output:
# default selected
#

# Program:

begin
  int value = 99;
  switch (value)
    case 1: println "first case"; Break
    case 2: println "second case"; Break
    default: println "default selected"
  end
end
