# for loop using continue and break

# Output:
# 1
# 3
# 5
# finished
#

# Program:

begin
  for (int i = 0, i < 10, i = i + 1)
    if i == 6 then Break else skip fi;
    if i % 2 == 0 then Continue else skip fi;
    println i
  done;

  println "finished"
end
