# find the max of two numbers

# Output:
# max value = 17
#

# Program:

begin
  int i = 0 ;
  int x = 10 ;
  int y = 17 ;

  for (skip, (y > 0 || x > 0), x = x - 1 ; y = y - 1 ; i = i + 1)
    skip
  done ;

  print "max value = ";
  println i
end