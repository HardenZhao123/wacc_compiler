# find the min of two numbers

# Output:
# min value = 10
#

# Program:

begin
  int i = 0 ;
  int x = 10 ;
  int y = 17 ;

  for (skip, y > 0 && x > 0, skip)
    x = x - 1 ;
    y = y - 1 ;
    i = i + 1
  done ;

  print "min value = " ;
  println i
end